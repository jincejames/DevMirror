# Pinned dependencies for Databricks Apps deployment.
# Keep in sync with pyproject.toml. Update by checking installed versions.

# Engine wheel produced by `scripts/build_app.sh` (or by the customer
# bundle's `artifacts.devmirror_wheel` build step) and dropped into
# `app/dist/`.  Pinned to the exact wheel filename because pip's
# requirements.txt format does not accept wildcards -- bump this line
# in lockstep with `pyproject.toml`'s `version =` field.
./dist/devmirror-0.2.10-py3-none-any.whl

fastapi==0.135.3
uvicorn[standard]==0.44.0
pyyaml==6.0.3
pydantic==2.12.5
databricks-sdk==0.96.0
